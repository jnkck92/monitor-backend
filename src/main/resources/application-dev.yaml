divera:
  accesskey: dein-test-key-hier
  base-url: ${DIVERA_BASE_URL}
  poll-interval-ms: 10000

configuration:
  path: ${INSTANCE_CONFIG_PATH:./local-config}
  tenants:
    - feuerwehr-test-1
    - feuerwehr-test-2